# Netlify redirects configuration
# This file is used by Netlify to handle URL routing without redirects

# Serve HTML files without .html extension (internal rewrite, no redirect)
/privacy    /privacy.html   200
/terms      /terms.html     200
/sitemap   /sitemap.html   200

